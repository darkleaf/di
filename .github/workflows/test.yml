name: Tests
on: [push]

jobs:
  tests:
    runs-on: ubuntu-latest
    container: clojure:openjdk-11
    steps:
      - uses: actions/checkout@v2
      - uses: actions/cache@v2
        with:
          path: |
            /root/.m2
            /root/.gitlibs
          key: test-${{ hashFiles('deps.edn') }}
          restore-keys: test-
      - run: clojure -X:test
