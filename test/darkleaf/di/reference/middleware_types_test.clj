;; # Middleware types

(ns darkleaf.di.reference.middleware-types-test
  (:require
   [clojure.test :as t]))

;; TODO: describe the registry-middleware abstraction
;; (`registry -> key -> Factory`) and list the built-in
;; middlewares: `di/update-key`, `di/env-parsing`,
;; `di/add-side-dependency`, `di/log`, `di/ns-publics`,
;; map form, seqable form.
