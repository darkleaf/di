# Middleware types

TODO: describe the registry-middleware abstraction
(`registry -> key -> Factory`) and list the built-in middlewares:
`di/update-key`, `di/env-parsing`, `di/add-side-dependency`,
`di/log`, `di/ns-publics`, the map form, and the seqable form.
